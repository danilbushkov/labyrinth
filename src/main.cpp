#include "actions.h"
#include "agent.h"
#include "environment.h"

using namespace std;




int main(){

    // Environment env = Environment();
    // Agent agent(env.getCountStates());
    // agent.saveQTable();
    // env.show();

    return 0;
}

